import RecipeService from '../Services/RecipeService';
const mockAxios = require('axios');

const searchArray = ['chocolate', 'milk'];

describe('RecipeService', () => {
  it.skip('should be a real test someday', () => {});
});
